"use strict";
function print(coors, coors2) {
    console.log(0);
}
function print2(coors) {
    console.log(0);
}
// В итоге в имплементированном interface будут оба эти значения a и b
// А type так не могут и выдают ошибку
// type TTest = {
// 	a: number;
// };
// type TTest = {
// 	b: number;
// };
// Каст типов
const cast = (point) => {
    const d = point;
};
const myCanvas = document.getElementById('canvas');
